# Per-grammar configuration for the delimiter-coexistence example.

# Give the element list a readable subject in the "Assuming … complete" hedge
# (the auto-derived form would be "the elems are complete").
[names]
elems = elements
