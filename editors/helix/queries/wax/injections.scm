; No language injections are defined for Wax: string and char literals carry no
; embedded sub-language. This file exists so consumers that expect an
; injections query find an (empty) one rather than erroring.
