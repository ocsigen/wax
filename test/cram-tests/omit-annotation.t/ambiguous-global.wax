type point = {x: i32, y: i32};
type vec = {x: i32, y: i32};

// As for a local, an ambiguous global initializer is reported once: the global
// becomes poison, so its use does not cascade into a second error.
const g = {x: 1, y: 2,};

fn use_g() -> &point {
    return g;
}
