; The Wax reference generator (see gen_llms.ml and ADOPTION.md Phase 6). Not
; installed; assembles the mdbook sources (docs/src) into the flat, single-file
; language reference served to AI assistants. The generated file is committed
; as skills/wax/reference.md — see skills/wax/dune for the sync rule.

(executable
 (name gen_llms)
 (modules gen_llms))

; Extracts the Wax snippets from docs/src/examples.md into a JSON array for the
; browser playground's "Examples" dropdown (see gen_examples.ml and
; PLAYGROUND.md). Not installed; run at docs-deploy time.

(executable
 (name gen_examples)
 (modules gen_examples))

; Extracts the Wax keyword list from src/lib-wax/lexer.ml into a JSON array for
; the browser playground's lexical highlighter (see gen_keywords.ml). Not
; installed; run at docs-deploy time.

(executable
 (name gen_keywords)
 (modules gen_keywords))
